#!/usr/bin/env bash

set -eo pipefail

macos_bootstrap="$(cd "$(dirname "$0")/.." && pwd -P)"

modules=(
  macos
  brew
  node
  ruby
  git
  ssh
  dotfiles
)

for module in "${modules[@]}"
do
  bash "$macos_bootstrap/modules/$module.bash"
done
